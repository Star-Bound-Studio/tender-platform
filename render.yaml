# render.yaml — Render Blueprint for auto-deploy
# Docs: https://render.com/docs/blueprint-spec

databases:
  - name: tender-db
    plan: free
    databaseName: tender_platform
    user: tp_user

services:
  - type: web
    name: tender-api
    runtime: python
    plan: free
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: tender-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: REDIS_URL
        value: ""
      - key: MEILI_URL
        value: ""
      - key: MEILI_KEY
        value: ""
